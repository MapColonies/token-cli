export const ISSUER = 'mapcolonies-token-cli';

export const DEFAULT_SPIN_TIMEOUT = 1500;

export const PRIVATE_KEY_FILE_NAME = 'privateKey.jwk';

export const PUBLIC_KEY_FILE_NAME = 'publicKey.jwk';
